# Picsmith 上线前审计 · 第二轮（2026-06-13）

> 范围：第一轮审计（docs/pre-launch-audit-2026-06-12.md）修复后的新增功能面——
> 异步文档理解管线、局部编辑、画布交互改版，以及未关闭的旧债。
> 所有结论先经代码核查（限流依赖、计费路径、同步调用、生产校验覆盖）再下判断。

## 发现与修复状态

### 🔴 高优先级

| # | 发现 | 状态 |
|---|---|---|
| A1 | `reference-docs` 端点零限流零计费，每次触发 2 次 Gemini 视觉调用——成本敞口 | ✅ 已修：5/小时 + 20/天双层限流 |
| A2 | `upload-binary` 无限流，本地磁盘可被填满（连带 SQLite 写挂） | ✅ 已修：60/小时限流 + 每用户 500MB 存储配额 |
| A3 | 重复上传同一文档无限堆积资产与解析成本 | ✅ 已修：sha256 去重，重复秒回 duplicate |
| B1 | `doc_parser.parse`（pypdf+PyMuPDF，CPU 数秒）在事件循环内同步执行，冻结全站请求 | ✅ 已修：`asyncio.to_thread`，缩略图构建同改 |
| B2 | 单进程任务引擎 + SQLite 单写者的部署约束无强制（`--workers 2` 即审批失联/锁死） | ✅ 已修：启动独占 flock，第二进程拒绝启动 |
| B4 | 生产校验缺 CORS（localhost 上线即前端全挂）、PROVIDER_MODE=mock 检查 | ✅ 已修：补两项 fatal + SQLite 软启动警告 |
| C1 | 文档图收进资产面板但入口是无文字小图标，用户看不到"AI 拿到了什么" | ✅ 已修：回执内嵌缩略图条（产品图标 P 角标），点击插入 `@asset_N` 引用，随消息持久化 |
| R1 | sub2api 两把 API key 曾在对话中明文出现 | ⚠️ 待用户：建议轮换 |
| R2 | Stripe 全链路未用真实 test key 冒烟 | ⚠️ 待用户提供 key（M3 收尾项） |

### 🟡 中优先级

| # | 发现 | 状态 |
|---|---|---|
| A4 | 文档回执/进度硬编码中文，与"海外先行"矛盾 | ✅ 已修：zh/en 文案表，lang 由前端语言设置透传 |
| A5 | 视觉理解失败静默降级，用户不知质量打折 | ✅ 已修：回执注明"AI 视觉理解未成功（基础模式）" |
| A6 | sessions/assets 列表无分页，重度用户线性变慢 | ✅ 已修：limit/offset（默认 200/500，上限 500），响应结构不变 |
| B3 | 1.2s 轮询放大系数（百人并发时 SQLite 读压力） | 📋 backlog：M4 迁 Postgres 时一并评估 SSE |
| B5 | 无任何备份机制，磁盘损坏=付费数据全失 | ✅ 已修：scripts/backup.sh（sqlite3 .backup 在线一致性 + storage 打包 + 保留 7 份 + crontab 示例） |
| C2 | 界面中英混搭（聊天中文、骨架英文） | ✅ 已修：design-agent 包接入双语 |
| C3 | 分钟级等待只有 3 个静止阶段文案，用户怀疑卡死 | ✅ 已修：45 秒心跳（"仍在阅读…已用时 N 秒"） |
| C4 | 产品无实拍图时静默虚构外观——电商场景高客诉点 | ✅ 已修：planner 规则强制在计划 notes 中警示并建议补图 |
| R3 | 用户上传品牌素材改图的版权责任 | 📋 待办：Terms 增补"素材合法性由用户保证"+ 投诉通道（M4 法务面） |
| R4 | 无监控告警（Sentry 等） | 📋 M4 部署项 |

### 🟢 低优先级

| # | 发现 | 状态 |
|---|---|---|
| C5 | 移动端走到画布才被告知不支持 | 📋 backlog：落地页 CTA 处给移动端预期 |
| C6 | 遮罩模式快捷键（ESC 等）无提示层 | ✅ 已修 |
| C7 | 资产 hover 只显示 label，浪费已有 AI caption | ✅ 已修：hover 显示描述 |
| R5 | 酒类案例在部分广告渠道受限 | 📋 营销注意项 |

## 总体判断

核心链路（异步任务、退款对账、文档理解、局部编辑）经过实测质量过硬；
本轮关闭的最大风险是**成本安全**（A1/B1：一个烧钱、一个卡死全站）。
剩余阻塞项全部依赖外部资源：API key 轮换确认、Stripe test key、域名/R2/Postgres（M4）。
